<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.FirstKotlinProject" parent="Theme.MaterialComponents.DayNight.NoActionBar.Bridge">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/app_light_gray</item>
        <item name="colorPrimaryVariant">@color/app_white</item>
        <item name="colorOnPrimary">@color/app_white</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <item name="android:windowLightStatusBar" tools:ignore="NewApi">true</item>

        <!-- Customize your theme here. -->


    </style>

    <style name="BottomNavigation">
        <item name="android:background">@drawable/bottom_bar_bg</item>
        <item name="itemIconTint">@drawable/navigation_bar_item_bg</item>
        <item name="itemTextColor">@drawable/navigation_bar_item_bg</item>
    </style>
</resources>